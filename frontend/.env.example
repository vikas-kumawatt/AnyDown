# Copy to .env.local for local dev, or set in Netlify's build environment.

# Backend base URL, no trailing slash.
VITE_API_BASE=http://127.0.0.1:8000

# Only if you set APP_APP_KEY on the backend.
# VITE_APP_KEY=
